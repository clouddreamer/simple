<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "何を書きます..." %>
  </div>
  <%= f.submit "発送", class: "btn btn-large btn-primary" %>
<% end %>